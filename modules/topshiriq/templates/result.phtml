<?php $this->layout('system::layout/shablon', [
  'title'      => $title,
  'page_title' => $title,
  ]) ?>
<style>.centered{ text-align: center; } .rep-img{ max-height: 100px; max-width: 100px; } .disabledbutton { pointer-events: none; opacity: 0.7; } .test {cursor: pointer;}</style>
<div class="container position-absolute top-50 start-50 translate-middle card card-body">
  <div class="centered">
    <h3 class="centered m-5"> 
      <?= $this->fetch('system::app/alert', [
        'alert_type' => $type == 'success' ? 'alert-success' : 'alert-danger',
        'alert'      => $message,
        ]) ?>
    </h3>
    <?php if (! empty($back_url)): ?>
    <a href="<?= $back_url ?>" class="btn btn-success fs-3 col-md-4 p-3 m-4"><?= __('orqaga') ?></a>
    <?php endif; ?>
  </div>
</div>